---
title: "Reservoir Sampling in One Pass"
description: "Placeholder post — short title, used to check how the index reads when a row does not wrap. Safe to delete."
date: 2026-06-14
---

> **Placeholder.** Delete every `src/content/posts/placeholder-*.mdx` when real
> content replaces them.

A short title, kept short on purpose: the striped index needs at least one row
that fits on a single line, so the row height and the baseline alignment of the
date column can be checked against rows that wrap.

The body is deliberately brief. Nothing here is real writing.
